# 在 Windows 上 checkout 时不要把这些文件转成 CRLF。
# entrypoint.sh 一旦是 CRLF，Alpine 的 sh 会直接报 "not found"。
* text=auto eol=lf

*.sh   text eol=lf
*.mjs  text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# 图片是二进制，不要做行尾转换
*.png binary
*.jpg binary
*.ico binary
